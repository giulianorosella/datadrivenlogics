% test1 file for prover9/mace4

%this formula is not a theorem of classical logic nor intuitinistic logic

%if we run prover9, we must obtain a search failed for time limit

%if we run mace4, we must obtain a counterexample

formulas(goals).

X ^ '(X) = 1.

end_of_list.