(0 * 0)=1.

%this formula is a theorem of both classical logic and intuitinistic logic

%if we run prover9, we must obtain a proof

%if we run mace4, we must obtain a search failed for time limit

formulas(goals).

INSERT_FORMULA_HERE

end_of_list.
